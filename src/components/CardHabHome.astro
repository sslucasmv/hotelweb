---

import { Image } from 'astro:assets';

//src/compoments/CardHabHome.astro

const { imagen, titulo, descripcion } = Astro.props;

---

  <div class="group">
    <div class="aspect-[4/3] rounded-xl overflow-hidden mb-4">
        <Image 
            src={imagen} 
            alt={titulo}
            width={800}    /* Ancho máximo real para escritorio */
            height={600}   /* Mantiene el aspecto 4:3 que usás en el div */
            widths={[400, 800]} /* Genera una versión chiquita para celus */
            sizes="(max-width: 600px) 400px, 800px" /* El navegador elige cuál bajar */
            format="webp"  /* Forzamos el formato liviano */
            class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
        />
    </div>
    <h3 class="text-xl font-bold mb-2 text-gray-dark">{titulo}</h3>
    <p class="text-gray-600 leading-relaxed">{descripcion}</p>
</div>